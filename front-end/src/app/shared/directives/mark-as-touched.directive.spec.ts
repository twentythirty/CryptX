import { MarkAsTouchedDirective } from './mark-as-touched.directive';

describe('MarkAsTouchedDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkAsTouchedDirective();
    expect(directive).toBeTruthy();
  });
});
